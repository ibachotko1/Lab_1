<Page x:Class="Warehouse.Front.Pages.HelpPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="HelpPage"
      Background="White"
      FontFamily="Segoe UI"
      FontSize="14">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Справка" FontSize="22" FontWeight="Bold" Margin="0,0,0,12"/>

        <ComboBox x:Name="HelpTopicCombo" Grid.Row="1" Width="300" Height="28" 
                  SelectionChanged="HelpTopicCombo_SelectionChanged" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="HelpContentText" TextWrapping="Wrap" TextAlignment="Left"/>
        </ScrollViewer>
    </Grid>
</Page>